export type ThemeOption = "light" | "dark" | "birindar" | "ugly" | "bad";

export type ThemeConfig = {
  primary: string;
  background: string;
  nav: string;
  border: string;
  text: string;
  sidebar: string;
};
